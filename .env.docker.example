# =====================================================
# Diet Planner - Docker Environment Configuration
# =====================================================
# Copy this file to .env.docker and fill in your values
# NEVER commit .env.docker to version control!
# =====================================================

# =====================================================
# DATABASE CONFIGURATION
# =====================================================
# These values are used by both PostgreSQL container and backend
POSTGRES_USER=diet_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=fitness_track

# =====================================================
# SECURITY
# =====================================================
# Generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here_generate_a_strong_random_string
ALGORITHM=HS256

# =====================================================
# LLM CONFIGURATION (Ollama)
# =====================================================
# Ollama runs on your host machine, not in Docker
# The container accesses it via host.docker.internal:11434
OLLAMA_API_KEY=your_ollama_api_key_here
OLLAMA_MODEL=gpt-oss:120b-cloud

# =====================================================
# LANGFUSE (Optional - LLM Observability)
# =====================================================
# Leave empty if not using Langfuse
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_HOST=https://us.cloud.langfuse.com

# =====================================================
# OPENROUTER (Optional - Alternative LLM)
# =====================================================
# Uncomment if using OpenRouter instead of Ollama
# OPENROUTER_API_KEY=your_openrouter_api_key_here

# =====================================================
# QDRANT (Optional - API Key)
# =====================================================
# Leave empty for local Qdrant without authentication
# QDRANT_API_KEY=
